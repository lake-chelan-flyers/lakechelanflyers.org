# Building and running

Run `make build` to build the Docker image that builds and serves the site.

Run `make serve` to run the Docker image and start the local web server.

Do not run `make serve` in the background. Ask the user to run `make serve` if
you need to query the local web server.
